函数input()让程序暂停执行，等待用户输入一些文本，获取用户输入后，Python将其赋值给变量方便后续使用
函数input()接受一个参数 即要向用户显示的提示的提示或说明
使用函数input()时，Python将用户输入解读为字符串

函数int()让Python将输入视为数值
函数int()将数的字符串表示转换为数值表示

while循环不断执行，直到指定的条件不满足为止

在要求很多条件都满足才继续运行的程序中，可定义一个变量，用于判断整个程序是否处于活动状态
这个变量称为标志flag，充当程序的交通信号灯
可以在while语句中通过检查flag的值来决定是否继续循环

